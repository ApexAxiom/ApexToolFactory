version: 1.0
runtime: nodejs18
build:
  commands:
    pre-build:
      - corepack enable
      - corepack prepare pnpm@8.15.4 --activate
      - pnpm install --frozen-lockfile
    build:
      - pnpm build
run:
  command: node scripts/start.js
  network:
    port: 8080
