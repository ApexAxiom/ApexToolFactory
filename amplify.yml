version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Static site - no preBuild needed"
    build:
      commands:
        - echo "Static site - no build needed"
  artifacts:
    baseDirectory: public
    files:
      - "**/*"
  cache:
    paths: []
